# The vendored mini-swe-agent harness is imported from third_party/ directly.
# These are its small runtime dependencies; no upstream CLI installation is
# required. Claude Code/Codex remain optional external agent backends.
pyelftools>=0.30
pydantic>=2.0
jinja2>=3.1
litellm>=1.75.5,!=1.82.7,!=1.82.8
tenacity>=8.0
rich>=13.0
python-dotenv>=1.0
platformdirs>=4.0
